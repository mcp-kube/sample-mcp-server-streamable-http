apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

namespace: default

commonLabels:
  app.kubernetes.io/name: mcp-server-streamable-http
  app.kubernetes.io/version: "1.0.0"
