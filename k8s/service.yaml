apiVersion: v1
kind: Service
metadata:
  name: mcp-server-streamable-http
  labels:
    app: mcp-server-streamable-http
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: mcp-server-streamable-http
